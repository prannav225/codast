import chalk from 'chalk';

const white = chalk.white.bold;
const textMuted = chalk.hex("#676E95");
const textMain = chalk.hex("#EEFFFF");
const title = chalk.white.bold;
const rule = chalk.hex("#3B4261");

console.log("=== OPTION A (Solid Geometric Blocks) ===");
console.log(`  ${white("██    ██")}    ${title("Codast CLI 0.2.0")}`);
console.log(`    ${white("██  ██")}    ${textMuted("Local Code Intelligence & REPL")}`);
console.log(`  ${white("██      ")}    ${textMain("Neural AST & Semantic Index Active")}`);
console.log(`        ${white("██")}    ${textMuted("/Volumes/Mac T7/Projects/cmd-line-ai (main)")}`);
console.log(`  ${rule("─────────────────────────────────────────────────────────────────────────────")}`);

console.log("\n=== OPTION B (Stepped Slope Blocks + Lower Underscore) ===");
console.log(`  ${white("▄▄    ██")}    ${title("Codast CLI 0.2.0")}`);
console.log(`  ${white("████  ██")}    ${textMuted("Local Code Intelligence & REPL")}`);
console.log(`  ${white("▀▀      ")}    ${textMain("Neural AST & Semantic Index Active")}`);
console.log(`        ${white("▄▄")}    ${textMuted("/Volumes/Mac T7/Projects/cmd-line-ai (main)")}`);
console.log(`  ${rule("─────────────────────────────────────────────────────────────────────────────")}`);

console.log("\n=== OPTION C (Clean Chevron Angle + Dual Cursor) ===");
console.log(`  ${white("▶   ██")}      ${title("Codast CLI 0.2.0")}`);
console.log(`  ▶   ██      ${textMuted("Local Code Intelligence & REPL")}`);
console.log(`              ${textMain("Neural AST & Semantic Index Active")}`);
console.log(`      ${white("▀▀")}      ${textMuted("/Volumes/Mac T7/Projects/cmd-line-ai (main)")}`);
console.log(`  ${rule("─────────────────────────────────────────────────────────────────────────────")}`);
