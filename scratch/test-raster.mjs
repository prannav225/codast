import chalk from 'chalk';

const w = chalk.white.bold;
const m = chalk.hex("#676E95");
const t = chalk.white.bold;
const main = chalk.hex("#EEFFFF");

console.log("=== RASTER 1: High-Density 2x2 Sub-pixel Chevron ===");
console.log(`  ${w("██▄      ████")}    ${t("Codast CLI 0.2.0")}`);
console.log(`  ${w("  ▀██▄   ████")}    ${m("Local Code Intelligence & REPL")}`);
console.log(`  ${w("  ▄██▀       ")}    ${main("Neural AST & Semantic Index Active")}`);
console.log(`  ${w("██▀      ████")}    ${m("/workspace (main)")}`);

console.log("\n=== RASTER 2: Heavy Geometric Chevron + Block + Bar ===");
console.log(`  ${w("███▄       █████")}    ${t("Codast CLI 0.2.0")}`);
console.log(`  ${w("  ▀███▄    █████")}    ${m("Local Code Intelligence & REPL")}`);
console.log(`  ${w("  ▄███▀         ")}    ${main("Neural AST & Semantic Index Active")}`);
console.log(`  ${w("███▀       ▄▄▄▄▄")}    ${m("/workspace (main)")}`);

console.log("\n=== RASTER 3: Sloped Polygon Chevron ===");
console.log(`  ${w("█▀▄      ████")}    ${t("Codast CLI 0.2.0")}`);
console.log(`  ${w("  ▀█▄    ████")}    ${m("Local Code Intelligence & REPL")}`);
console.log(`  ${w("  ▄█▀        ")}    ${main("Neural AST & Semantic Index Active")}`);
console.log(`  ${w("█▄▀      ▄▄▄▄")}    ${m("/workspace (main)")}`);

console.log("\n=== RASTER 4: True 4-Row Solid Pixel Matrix ===");
console.log(`  ${w("██         ████")}    ${t("Codast CLI 0.2.0")}`);
console.log(`  ${w("  ████     ████")}    ${m("Local Code Intelligence & REPL")}`);
console.log(`  ${w("  ████         ")}    ${main("Neural AST & Semantic Index Active")}`);
console.log(`  ${w("██         ▄▄▄▄")}    ${m("/workspace (main)")}`);

console.log("\n=== RASTER 5: Seamless Continuous Chevron (Half-block Diagonal) ===");
console.log(`  ${w("█▄       ████")}    ${t("Codast CLI 0.2.0")}`);
console.log(`  ${w(" ▀█▄     ████")}    ${m("Local Code Intelligence & REPL")}`);
console.log(`  ${w(" ▄█▀         ")}    ${main("Neural AST & Semantic Index Active")}`);
console.log(`  ${w("█▀       ▄▄▄▄")}    ${m("/workspace (main)")}`);
